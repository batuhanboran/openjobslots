# OpenJobSlots ATS Parser Matrix

OpenJobSlots prefers direct public ATS APIs, durable cached payload metadata, and conservative validation. Third-party scraper projects may be useful references, but parser code should not be vendored until license, maintenance, and correctness are reviewed.

Every adapter must expose `detect`, `buildRequests`, `fetch`, `parse`, `normalize`, `validate`, `cacheKey`, `rateLimit`, and `fixtures`. Every normalized posting must fit the durable shape: `source_job_id`, `canonical_url`, `apply_url`, `title`, `company`, `location_text`, `country`, `region`, `remote_type`, `industry`, `posting_date`/`posted_at`, `first_seen`, `last_seen_epoch`, `ats_key`, `parser_version`, `raw_hash`, and `confidence`.

Parser changes must be checked against public search quality, not only parser unit output. When parser output changes title, location, country, region, remote type, posting date, hidden state, or canonical URL behavior, run the search corpus described in [Search Quality Runbook](./search-quality-runbook.md).

Missing location, posting date, or remote fields must be certified by source fixtures. A parser may leave those fields blank only when a saved raw response proves the source omitted them, or adapter notes document why extracting them would be unsafe or require rejected extra fetches.

For future Codex work, use `ats-parser-certification` for raw source and parser decisions, `openjobslots-postgres-audit` for DB/Meili/API parity, and `openjobslots-search-parity-corpus` for large title/filter matrices. These skills exist to prevent closing a parser incident from UI-only or count-only tests.

## May 24 Parser Module Layout

The canonical parser implementation path is now `server/ingestion/sources/<ats>/parse.js`. `server/index.js` remains as app bootstrap plus legacy collector/discovery/fetch orchestration and may call those parser modules until the next extraction phase. In the tables below, older `server/index.js` parser-path notes should be read as collector/orchestration notes unless the row has already been updated to the source module path.

For new work: add parser behavior only in the ATS source module, add or update raw/expected fixtures, and keep network fetch/discovery changes separate from parser normalization changes when possible.

## May 8 Priority Order

Parser normalization is the current blocker. Fix `location_text`, `country`, `region`, `remote_type`, `posting_date`, `source_job_id`, and `last_seen_epoch` per ATS before search-index tuning. v1.5.14 expands the high-volume parser batch to `workday`, `bamboohr`, `taleo`, `applytojob`, `breezy`, `recruitee`, `icims`, and `applitrack`, then adds safe URL/title backfill and ATS quality auditing. v1.5.15 applies that backfill to production, fully rebuilds Meilisearch from Postgres, and marks pre-reindex search outbox rows processed to avoid duplicate worker indexing. v1.5.16 broadens stable URL source-id extraction and global location aliases for the next production backfill. v1.5.17 adds the 60-source certification workbench and tests that block parser-certified status without raw fixture-backed field decisions. v1.5.18 stabilizes production backfill for source date epochs and conservative onsite classification from concrete physical locations while preserving stronger existing source IDs and remote types during upserts. v1.5.19 adds saved iCIMS/Applitrack raw detail fixtures, iCIMS ATS-code geo/remote parsing, Applitrack detail URL certification, and the dry-run-first detail-page backfill tool. v1.5.20 fixes conflicting iCIMS backfill country values exposed by live API/Postgres parity checks. v1.5.21 expands the normalized adapter contract fields, carries those fields through Postgres/Meili, and promotes ApplicantPro core jobs JSON to raw parser fixture-backed status. v1.5.22 targets the largest remaining live gaps by adding iCIMS JSON-LD detail extraction, Applitrack detail label/address recovery, and Manatal raw API certification. v1.5.24 promotes BambooHR, Recruitee, ApplyToJob, Breezy, and Zoho from normalized-only to strict saved raw parser-fixture-backed certification with invalid source-shape rejection fixtures. v1.5.25 adds ATS field-quality diagnostics, source-evidence-only department/employment backfill, and generic multi-location city guards for the top live gap group (`icims`, `applitrack`, `manatal`, `applytojob`, `hrmdirect`). v1.6 hardening promotes Greenhouse, Lever, Ashby, SmartRecruiters, Workday, and Taleo to strict raw parser-fixture-backed tests with invalid-shape rejection, while keeping Taleo low-confidence because tenant columns remain brittle. v1.6.1 adds guarded, resumable iCIMS/Applitrack detail refetch planning/apply mode with audit tables, host caps, explicit production safety flags, and no overwrite of stronger existing normalized values. Wave A repairs harden `icims`, `applitrack`, `manatal`, `taleo`, and `workday` parser fixtures without running a full DB rebuild or Meili replace reindex. Wave B repairs promote `hrmdirect` to strict raw parser-fixture-backed coverage and harden `smartrecruiters`, `zoho`, `bamboohr`, `ashby`, and `greenhouse` field extraction for future rows. The direct JSON/API repair wave adds dedicated source modules for `greenhouse`, `lever`, `ashby`, `smartrecruiters`, `recruitee`, `bamboohr`, `manatal`, `recruitcrm`, `pinpointhq`, `fountain`, and `zoho`, so runtime source canaries now use ATS-specific discover/fetch/parse/normalize/validate functions with local invalid-shape fixtures. Image work and build-cache cleanup are later tasks.

Current live production data-quality finding before v1.5.13: 722,591 active postings; 625,905 were missing `country`/`region`; 96,686 had `country`/`region`. After the v1.5.15 production backfill and Meili reindex, 725,071 visible rows remain indexed, 342,008 are still missing `country`, and 343,773 are still missing `region`. A May 8 pre-v1.5.16 audit still found high missing-source-id counts in ATS outside the first parser batch, especially `manatal`, `hrmdirect`, `pinpointhq`, `zoho`, `jobvite`, `greenhouse`, `smartrecruiters`, `teamtailor`, and `lever`; v1.5.16 adds parser/backfill source-id recovery for those URL shapes. v1.5.19 production preflight found iCIMS still had 24,933 `CC-state-city` rows and 146 `CC-Remote` rows; those are normalization/backfill targets. Applitrack remained 70,550/70,550 missing geo/date because existing stored rows lack list evidence and need polite detail-page backfill. v1.5.25 preflight ranked remaining field gaps by ATS: `icims`, `applitrack`, `manatal`, `applytojob`, and `hrmdirect` are the highest-impact safe batch; cache metadata cannot repair their geo/date fields, so the safe automatic backfill is limited to city from concrete location text and department/employment fields already present in source-derived stored data.

## Parser Tiers

| Tier | ATS keys | Parse rule | Fixture status |
| --- | --- | --- | --- |
| Direct JSON stable | `greenhouse`, `lever`, `ashby`, `smartrecruiters`, `recruitee`, `bamboohr`, `teamtailor`, `freshteam`, `pinpointhq`, `recruitcrm`, `fountain`, `getro` | Prefer public JSON job-board endpoint, parse pagination before HTML fallback, cache response hash/metadata. | Strict parser-backed now: `greenhouse`, `lever`, `ashby`, `smartrecruiters`, `bamboohr`, `recruitee`, `pinpointhq`, `recruitcrm`, `fountain`. Dedicated source modules now exist for those plus `manatal` and `zoho`; pending: `teamtailor`, `freshteam`, `getro`. |
| Enterprise/direct | `workday`, `oracle`, `adp_myjobs`, `adp_workforcenow`, `paylocity`, `dayforcehcm`, `eightfold`, `saphrcloud`, `ultipro`, `pageup` | Extract tenant/site identifiers, prefer candidate API responses, normalize product-specific dates and locations. | Strict parser-backed/source-module now: `workday`, `oracle`, `paylocity`, `adp_workforcenow`, `adp_myjobs`, `ultipro`, `pageup`, `saphrcloud`; `eightfold` pending; `dayforcehcm` unsupported. |
| Embedded or semi-structured | `jobvite`, `icims`, `zoho`, `breezy`, `applicantpro`, `applytojob`, `theapplicantmanager`, `careerplug`, `talentreef`, `hirebridge`, `hrmdirect`, `isolvisolvedhire` | Extract embedded JSON first, then conservative DOM card parsing only when URL/title/company are reliable. | Strict parser-backed now: `applicantpro`, `applytojob`, `breezy`, `careerplug`, `hrmdirect`, `icims`, `zoho`. Raw parser fixtures pending for the rest. |
| Vendor-specific | `applicantai`, `gem`, `join`, `careerspage`, `manatal`, `hibob`, `sagehr`, `loxo`, `peopleforce`, `simplicant`, `rippling`, `careerpuck`, `talentlyft`, `talexio` | Use vendor-specific public payloads where stable; otherwise reject ambiguous postings. | Strict parser-backed now: `manatal`. Pending fixtures for the rest. |
| Public sector / education | `governmentjobs`, `usajobs`, `k12jobspot`, `schoolspring`, `calcareers`, `calopps`, `statejobsny`, `policeapp`, `jobaps`, `applitrack` | Preserve agency/school location fields, enforce polite pagination, avoid mixing aggregate board URLs with canonical apply URLs. | Strict parser-backed now: `applitrack`; raw parser fixtures are pending for the rest. |
| Brittle / high risk | `taleo`, `brassring` | Keep low confidence until fixtures prove stability; rate-limit heavily and reject ambiguous records. | `taleo` and `brassring` now have raw source-module fixtures and invalid-shape coverage, but both remain low-confidence/quarantine-first. |

## Certification Gates

Enterprise/brittle source modules now follow the same runtime contract as the direct JSON/API wave: per-source `discover`, `fetchList`, `fetchDetail`, `parse`, `normalize`, `validate`, raw fixture, expected normalized fixture, and invalid-shape fixture. These modules do not imply production promotion by themselves; brittle sources remain canary-first and quarantine ambiguous rows until live bad-row rates stay below threshold.

- Saved raw response fixture and expected normalized fixture for each ATS.
- Validation rejects missing URL, company, or title.
- Parser documents endpoint or URL pattern, pagination, date/location parsing, remote/hybrid handling, failure modes, confidence, and rate limit.
- Expected normalized fixtures include `location_text`, `country`, `region`, `remote_type`, `posting_date`/`posted_at`, `source_job_id`, and `last_seen_epoch`.
- Missing or nullable `location_text`, `country`, `region`, `remote_type`, or posting-date fields are explained by raw source fixtures, not only normalized output.
- `source_job_id` preserves the strongest source id when the raw response exposes one.
- `last_seen_epoch` is present for freshness and pruning.
- Cache key includes ATS key and company URL; posting key is canonical URL.
- New ATS cannot be enabled by default until raw fixture tests and production parity tests pass.

## Required 60-ATS Adapter Matrix (v1.6.0)

This table is intentionally conservative. `certified` means the ATS has raw parser-fixture-backed tests in this repo. Normalized fixtures alone are marked `partial` because they do not prove the real source parser.

| ats_key | source type | adapter status | fixture status | parser confidence | known failure modes | DB normalization notes | next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `workday` | enterprise API | certified | raw parser fixture-backed | medium | CXS pages parse source id/location/date; relative dates remain fetch-time dependent. | Structured CXS location objects now preserve source city/country before URL fallback; JR/requisition ids and absolute external paths are preserved. Work-from-home URL fragments no longer become city values. | Add more pagination and detail-description fixtures. |
| `ashby` | public JSON API | certified | raw parser fixture-backed | medium | Current collector uses hosted GraphQL; official public API shape is also covered by raw fixture. | Remote/country may be nested in location/address objects; current hosted query can omit posting date. | Add live tenant GraphQL fixture variants with sparse secondary locations. |
| `greenhouse` | public JSON API | certified | raw parser fixture-backed | medium | Job Board API list fixture covers jobs[] location, department, content, source id, first-published date, and tracking-query canonicalization. | Preserve `id`/`gh_jid`; strip tracking query params; prefer `first_published` over `updated_at`; derive country from office/location text without storing full office text as country. | Add retrieve-job fixture only if detail questions/pay transparency become indexed fields. |
| `lever` | public JSON API | certified | raw parser fixture-backed | medium | Postings API fixture covers categories, allLocations, createdAt, hostedUrl/applyUrl, country code, and tracking-query canonicalization. | Preserve Lever id; broad regions can remain country-null when no country code/source location exists. | Add pagination skip/limit fixture. |
| `jobvite` | embedded JSON / HTML scrape | fallback | missing raw fixture | pending-fixture | HTML/embedded payload variants can hide location/date/source id. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `applicantpro` | embedded JSON / HTML scrape | certified | raw parser fixture-backed | medium | Domain-id discovery plus jobs JSON; raw parser fixture added. | Persist source id from JSON id; geo/date from jobLocation/startDateRef; remote from title/location evidence. | Monitor live parser attention and add bad-shape fixture for success=false/domain-id misses. |
| `applytojob` | embedded JSON / HTML scrape | certified | raw parser fixture-backed | medium | HTML list parser can recover title/url/location/date/employment type when card labels exist; rows without title or usable apply URL are rejected/skipped. | Source id comes from `/apply/{id}`; country/date/department/employment type are persisted when list labels expose them. | Add detail-page fixtures for legacy pages that omit date or department on list cards. |
| `theapplicantmanager` | embedded JSON / HTML scrape | fallback | missing raw fixture | pending-fixture | HTML/embedded payload variants can hide location/date/source id. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `breezy` | embedded JSON / HTML scrape | certified | raw parser fixture-backed | medium | HTML card parser handles `/p/{id}` URL, title, location/date/department in saved raw fixture; cards without a title or posting URL are rejected/skipped. | Source id comes from `/p/{id}`; country/date/remote are persisted when card text exposes them. | Add more board/detail fixtures for source variants that omit date/geo from cards. |
| `icims` | embedded JSON / HTML scrape | certified | raw parser fixture-backed | medium | High existing geo/date/remote gaps; raw detail fixtures now include iCIMS JobPosting JSON-LD date/location evidence plus guarded detail-refetch fixtures for geo and explicit remote labels. | Country-code locations such as `US-PA-Philadelphia` and `CA-ON-Toronto` now derive city when source proves it; JSON-LD detail pages can recover `datePosted` and postal address country/city. Guarded refetch uses detail location evidence for geo normalization without overwriting stronger stored `location_text`. | Run `refetch:details:dry-run` first, then guarded `refetch:details` only after backup and max-update approval. |
| `zoho` | embedded JSON / HTML scrape | certified | raw parser fixture-backed | medium | Hidden jobs JSON can omit id/title; missing id rows are skipped and missing titles are rejected. | Source id comes from hidden job id; city/state/country, date opened, and industry persist when payload exposes them. | Add malformed hidden JSON and unpublished job fixtures before broad live backfill. |
| `applicantai` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `gem` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `jobaps` | public sector feed / HTML scrape | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `join` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `talentreef` | embedded JSON / HTML scrape | fallback | missing raw fixture | pending-fixture | HTML/embedded payload variants can hide location/date/source id. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `careerplug` | HTML scrape | certified | raw parser fixture-backed | medium | Source can omit `.job-title` and expose the real title only through `aria-label`; generic `Untitled Position` and blank-title cards must be rejected. | Parser now uses class title first, then cleaned `aria-label`; URL id from `/jobs/{id}` is preserved; dates stay null unless a future detail fixture proves source date evidence. | Monitor live parser attention by grouped rejection reason; add detail fixture only if CareerPlug detail pages expose posting dates. |
| `bamboohr` | public JSON API | certified | raw parser fixture-backed | medium | Public API fixture covers location/date/source id when source exposes fields; rows without id and URL are skipped. | Source id comes from API id or `/careers/{id}` URL; object/string locations, city, dates, department, and remote fields persist when present. Remote-only labels are not stored as city. | Add tenant variants for sparse string-only locations before backfill. |
| `adp_myjobs` | enterprise API | partial | raw source-module fixture-backed | medium-low | Tenant discovery, pagination, and field nesting vary by customer. | Parser now emits `reqId` as source id and preserves structured city/state/country when source arrays expose them. | Run bounded canary before public promotion; add pagination/count variants. |
| `adp_workforcenow` | enterprise API | certified | raw source-module fixture-backed | medium | Raw direct fixture exists. | Parser now emits `itemID` as source id and preserves structured city/state/country separately from office labels. | Track legacy alias adpworkforcenow in DB separately; add more sparse location variants. |
| `oracle` | enterprise API | certified | raw source-module fixture-backed | medium | Raw parser fixture for CandidateExperience API. | Parser now carries requisition `Id` into source id; tenant discovery still brittle. | Add pagination and language variants. |
| `paylocity` | enterprise API | certified | raw source-module fixture-backed | medium | Raw fixture for page-data jobs JSON. | Parser now carries `JobId` into source id; embedded data can change shape. | Add more missing-field failure fixtures. |
| `eightfold` | enterprise API | fallback | missing raw fixture | medium-low-pending-fixture | Tenant discovery, pagination, and field nesting vary by customer. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `manatal` | public JSON API / vendor endpoint | certified | raw parser fixture-backed | medium | Public jobs API can omit posting dates; rows without hash/url are skipped and blank titles are rejected. | Parser carries city/state/country, department, employment type, description text, remote/hybrid evidence, source id from hash/id, and date fields when `published_at`/`last_published_at` are present. HTML fallback now preserves source id and explicit remote/hybrid location prefixes. | Backfill source-evidenced fields for existing Manatal rows, then reindex. |
| `careerspage` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `dayforcehcm` | enterprise API | disabled | unsupported | unsupported | Configured but unsupported. | Disabled from active sync until a public endpoint, fixtures, and rate-limit policy exist. | Keep disabled or implement from source-backed fixtures. |
| `pageup` | enterprise API / HTML results | partial | raw source-module fixture-backed | medium-low | Tenant discovery, pagination, and field nesting vary by customer. | Source module certifies list-row title/url/location/date parsing and URL id preservation. | Add paired detail-page fixture before broad backfill. |
| `hirebridge` | embedded JSON / HTML scrape | fallback | missing raw fixture | pending-fixture | HTML/embedded payload variants can hide location/date/source id. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `brassring` | HTML scrape / brittle enterprise board | partial | raw source-module fixture-backed | low | Brittle board shape; parser drift likely even with fixtures. | Parser now emits `reqid` as source id and quarantines ambiguous rows through source-module gate. | Keep low confidence; run source canary before public promotion. |
| `applitrack` | public sector feed / HTML scrape | certified | raw parser fixture-backed | medium | List output often omits geo/date; detail page can recover fields within budget, including inline label blocks, district address fallback, and explicit Work Type/Remote labels. | Existing rows need detail-page backfill; do not invent geo/date from blank list rows. The parser now supports single- and double-quoted `applyFor(...)`, extra arguments, `Location(s)`/building/worksite labels, and explicit negative remote text. Generic `DISTRICT WIDE` detail locations use source footer address only when present. Guarded refetch records HTTP/parser outcomes in audit tables. | Run `refetch:details:dry-run` first, then guarded `refetch:details` only after backup and max-update approval; add more tenant fixtures as variants appear. |
| `hibob` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `isolvisolvedhire` | embedded JSON / HTML scrape | fallback | missing raw fixture | pending-fixture | HTML/embedded payload variants can hide location/date/source id. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `teamtailor` | public JSON API | fallback | missing raw fixture | medium-pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `freshteam` | public JSON API | fallback | missing raw fixture | medium-pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `sagehr` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `loxo` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `peopleforce` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `simplicant` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `pinpointhq` | public JSON API | certified | raw parser fixture-backed | medium | Needs raw source fixture to prove parser field completeness. | Normalized shape includes source id, geo/date/remote when source exposes them. | Add more live-sampled failure fixtures before expanding parser claims. |
| `recruitcrm` | public JSON API | certified | raw parser fixture-backed | medium | Needs raw source fixture to prove parser field completeness. | Normalized shape includes source id, geo/date/remote when source exposes them. | Add more live-sampled failure fixtures before expanding parser claims. |
| `rippling` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `careerpuck` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `fountain` | public JSON API | certified | raw parser fixture-backed | medium | Needs raw source fixture to prove parser field completeness. | Normalized shape includes source id, geo/date/remote when source exposes them. | Add more live-sampled failure fixtures before expanding parser claims. |
| `getro` | public JSON API | fallback | missing raw fixture | medium-pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `governmentjobs` | public sector feed / HTML scrape | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `smartrecruiters` | public JSON API | certified | raw parser fixture-backed | medium | Public search fixture covers content[] id, company, structured location, shortLocation, releasedDate, employment type, department, and remote flag. | Structured city/region/country are preserved even when `shortLocation` says `Remote`; parser rejects missing title/company/url. Existing rows need reparse/backfill before public counts improve. | Add authenticated Posting API fixture only after token handling is reviewed. |
| `policeapp` | public sector feed / HTML scrape | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `usajobs` | public sector feed / HTML scrape | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `k12jobspot` | public sector feed / HTML scrape | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `schoolspring` | public sector feed / HTML scrape | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `calcareers` | public sector feed / HTML scrape | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `calopps` | public sector feed / HTML scrape | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `statejobsny` | public sector feed / HTML scrape | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `hrmdirect` | embedded JSON / HTML scrape | certified | raw parser fixture-backed | medium-low | HTML row/cell class variants can hide location/date/source id; off-host links are skipped. Some tenants expose grouped div list layouts instead of table rows. Some tenants expose `Location:` only on `req` detail URLs, while `req_loc`/`cust_sort*` filtered detail URLs can return blank labels. Other multi-location tenants expose the useful `Location:` only when `req_loc` is preserved. Some tenants expose `openings.php` instead of `job-openings.php`, state-, province-, country-only, office-prefix/scope, placeholder titles, or ambiguous multi-city values. RSS feeds may be large but expose `pubDate` tied to `req` item links or guid values. Stale or removed detail pages are reported as `detail_404_or_410`. | Parser skips exact `Apply Today` placeholder titles, carries `req` source id for single-`req` rows and `req:req_loc` source id when one `req` appears on multiple canonical URLs, parses grouped div rows through bounded detail `Location:` evidence, city/state-derived location, exact US state abbreviations from list `td.state` or detail `Location:` as United States country evidence without publishing the state code as city, `td.custSort1` work-mode/location evidence, `td.cities` remote-scope values such as `Remote - Colorado` or `Remote, Continental U.S.` as remote evidence without storing `Remote` as city, `td.offices` and detail `Office:` exact remote/hybrid, exact/prefixed country/state, and scoped remote evidence such as `Corporate Poland`, `Field UK Onshore`, `Corporate US Remote`, `WA - Remote`, or `Remote - Texas`, detail `Location:` remote-scope values such as `Remote - Arizona, AZ` or `Remote Texas` as remote evidence plus scoped location text after stripping the remote prefix, labeled detail `Workplace Type` remote/hybrid evidence, exact detail `Location: Remote/Hybrid` labels as remote evidence without storing them as geo/location, exact detail `#LI-Remote/#LI-Hybrid` remote evidence, detail body `Location:` remote/hybrid-role labels, exact detail body work-arrangement/work-environment/work-mode tags including `100% remote - work from home`, detail body `Location:` US street-address labels converted to `City, ST`, duplicate-`req` `req_loc` detail `Location:` evidence, RSS `pubDate` posting-date evidence matched by base `req` from item `link` or `guid` for both `job-openings.php` and `openings.php`, labeled detail `Date Posted/Posted Date/Posting Date/Open Date` date evidence when list/RSS dates are absent, country where state/location/office proves it, department, and job type. Source fetch uses `search=true`, optionally fetches `rss.php?search=true`, strips filtered detail params for normal rows, detects duplicate `req` from posting URLs before choosing `req_loc` detail fetches, reports detail 404/410 as quarantine evidence, and raises the bounded detail budget for sparse boards. Remote-only city/detail labels, exact state abbreviations, and ambiguous values such as `Multiple Cities` are not stored as city or fake geo; title-only Remote/Virtual text and generic body prose stay quarantined without labeled geo/remote evidence, exact remote tags, exact body labels, or strict address labels, and source-backed `onsite` values do not pass the public gate when useful geo is absent. | Add more tenant variants for onsite-without-geo quarantine samples before broad backfill; RSS/detail posting dates are source evidence, but do not invent dates when a source-labeled date for the base `req` is absent. |
| `talentlyft` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `talexio` | vendor public endpoint / unknown | fallback | missing raw fixture | pending-fixture | Needs raw source fixture to prove parser field completeness. | Keep nullable geo/date/remote/source fields unless raw source evidence proves values. | Add source docs/pattern, raw fixture, expected normalized fixture, and parser test. |
| `saphrcloud` | enterprise API | partial | raw source-module fixture-backed | medium-low | Tenant discovery, pagination, and field nesting vary by customer. | API parser now handles object-valued text arrays and preserves `id` as source id. | Add HTML fallback fixture and pagination payload variants. |
| `recruitee` | public JSON API | certified | raw parser fixture-backed | medium | PublicApp parser handles localized countries/departments; missing translated title is rejected instead of using slug as title. | Source id comes from offer id/uuid; localized country, department, date, and workplace type persist when present. | Add more language and location variants for tenants with sparse dates. |
| `ultipro` | enterprise API | partial | raw source-module fixture-backed | medium-low | Tenant discovery, pagination, and field nesting vary by customer. | Parser now extracts `Id`/`opportunityId` as source id and URL extraction recognizes `opportunityId`. | Add pagination/count and remote/hybrid structured variants. |
| `taleo` | HTML scrape / brittle enterprise board | certified | raw parser fixture-backed | low | REST and AJAX fixtures cover unstable columns, remote-title false positives, alternate date formats, and boolean-date rejection; tenant variants remain brittle. | Low confidence; parse only source-backed date/location columns and preserve jobId/contestNo. Remote words in job titles are no longer treated as location evidence unless the field is a standalone or delimited remote location value. | Add more tenant-specific REST/AJAX column variants before broad backfill. |

## Current Coverage Snapshot

- Configured ATS keys: 60.
- Fixture-backed parser output: `greenhouse`, `lever`, `ashby`, `smartrecruiters`, `recruitee`, `bamboohr`, `applicantpro`, `applytojob`, `breezy`, `hrmdirect`, `icims`, `zoho`, `applitrack`, `pinpointhq`, `recruitcrm`, `fountain`, `paylocity`, `oracle`, `adp_workforcenow`, `careerplug`, `manatal`, `workday`, `taleo`.
- Strict raw parser-backed adapters/tests: `adp_workforcenow`, `applicantpro`, `applitrack`, `applytojob`, `ashby`, `bamboohr`, `breezy`, `careerplug`, `fountain`, `greenhouse`, `hrmdirect`, `icims`, `lever`, `manatal`, `oracle`, `paylocity`, `pinpointhq`, `recruitcrm`, `recruitee`, `smartrecruiters`, `taleo`, `workday`, `zoho`.
- Source-module fixture-backed but still partial/canary-first: `adp_myjobs`, `brassring`, `pageup`, `saphrcloud`, `ultipro`.
- Certification blocker: normalized fixtures are useful, but they do not prove raw ATS HTML/JSON parsing. Each source still needs raw-response fixtures before it is considered fully certified.
- Legacy fetch dispatcher gaps found: canonical `ashby` did not map to the legacy Ashby collector; the adapter now fetches as `ashbyhq`.
- Known unsupported configured source: `dayforcehcm` has metadata and is visible as an ATS, but no collector implementation exists yet. It is disabled by default for sync, and the adapter fails explicitly with `parser_adapter_not_implemented` if called.
- Parser attention should count typed parser errors only: `parser_validation`, `parser_parse`, `parser_normalize`, and `parser_adapter_not_implemented`. Fetch/network failures remain run errors but should not inflate parser attention.

## Wave 2 Certification Notes

| ATS | Status | Confidence | Parser attention notes |
| --- | --- | --- | --- |
| `applytojob` | Fixture-backed for parsed list and legacy Resumator-style URL/location output. | Medium; HTML class names are still semi-structured. | Missing title or URL is a parser bug; empty result after a non-OK request is fetch/network. |
| `breezy` | Fixture-backed for `/p/` posting cards, department, location, and posted date output. | Medium; card HTML parsing is conservative. | Missing `h2` title or `/p/` URL is parser attention; portal fetch failure is network. |
| `hrmdirect` | Raw fixture-backed for `reqitem` rows with city/state/country, exact Apply Today placeholder-title skipping, grouped div list rows, `openings.php` route RSS dates, list state-abbreviation labels, list remote-scope city labels, list/detail Office remote/state/province/country/prefix/scope labels including `Remote - Texas`, detail Location state-abbreviation labels, detail Location remote labels, detail Location remote-scope labels, detail ambiguous multiple-city labels, detail body address Location labels, detail body work-arrangement/work-mode remote labels, detail date labels, exact LI remote tags, body Location remote labels, stale detail quarantine, department, RSS link/guid pubDate, job type, source id, off-host link skipping, and remote-city guard. | Medium-low; row class and cell classes are brittle. | Empty rows after successful fetch can be parser drift; request failure is network. |
| `icims` | Raw fixture-backed for iCIMS job cards/detail fields with `CC-state-city`, `CC-Remote`, and explicit `Remote: Yes/No`. | Medium; public dates often absent and pagination discovery remains brittle. | Bad job-card/detail extraction is parser; iframe/page fetch failure is network. |
| `zoho` | Fixture-backed for hidden jobs payload output with department and built Careers URL. | Medium; hidden JSON payload is stable when present. | JSON parse failure or missing job id is parser; missing careers page is network/source. |
| `applitrack` | Raw fixture-backed for `applyFor(jobId, category, specialty)` output plus `JobPostings/view.asp` detail date/location/remote evidence. | Medium; old rows with blank list evidence require budgeted detail-page backfill. | No `applyFor` matches after successful `Output.asp` fetch is parser drift; detail fetch failure is network/rate-limit. |

## Wave 3 Certification Notes

| ATS | Status | Confidence | Parser attention notes |
| --- | --- | --- | --- |
| `pinpointhq` | Fixture-backed from `postings.json` API response through parser and normalizer. | Medium; direct JSON fields are stable but location/departments can be sparse. | Missing `url` or `path` after a successful API response is parser/source drift. |
| `recruitcrm` | Fixture-backed from public jobs API response through parser and normalizer. | Medium; remote flag and slug fallback are covered. | Missing `data.jobs`, missing slug/url, or bad date fields are parser/source drift. |
| `fountain` | Fixture-backed from board `.json` openings response. | Medium; direct JSON is clear, but board URL parsing depends on `/c/{company}/...` path shape. | Empty `openings` with HTTP 200 is source/parser attention; HTTP failure remains fetch. |
| `paylocity` | Fixture-backed from extracted page-data JSON `Jobs` array. | Medium-low; source is direct JSON embedded in a page, and collector requires `PublishedDate`. | Missing `Jobs` or `JobId` is parser/source drift; landing page fetch is network. |
| `oracle` | Fixture-backed from CandidateExperience requisition API response. | Medium-low; parser handles nested `requisitionList`, but tenant/site/language discovery remains brittle. | Missing `requisitionList`, `Id`, `Title`, or `PostedDate` is parser/source drift. |

## Wave 3 Deferred Blockers

- `teamtailor`: current collector parses HTML job cards, not the Teamtailor API. Certification should wait for saved board HTML or a direct API endpoint fixture; expected output is title, canonical job URL, department, and location from metadata spans.
- `getro`: current collector extracts jobs from `__NEXT_DATA__` embedded in HTML. It is usable but not direct JSON; certification should include a saved Next.js page fixture and expected `initialState.jobs.found` output.
- `workday`: pure CXS `jobPostings` parser and saved raw fixture now certify source id, URL, location, date text, and remote evidence for list-visible fields. Add pagination and detail-description fixtures before claiming complete Workday enrichment.
- `pageup`: current collector parses AJAX result HTML and then fetches each details page to confirm posting date. Certification needs two-part fixtures: search results HTML plus detail page HTML.

## Research Anchors

- Greenhouse Job Board API: https://developer.greenhouse.io/job-board.html
- Ashby public job posting API: https://developers.ashbyhq.com/docs/public-job-posting-api
- Lever postings API: https://github.com/lever/postings-api
- SmartRecruiters Posting API: https://developers.smartrecruiters.com/docs/posting-api
- SmartRecruiters Search jobs API: https://developers.smartrecruiters.com/reference/jobsall-1
- Meilisearch search/indexing: https://github.com/meilisearch/meilisearch
- pg-boss queueing on Postgres: https://github.com/timgit/pg-boss

## Expansion Priority

Wave 1 candidates must be certified before enabling: Personio XML feed, Trakstar Hire / Recruiterbox frontend API, and JobScore feed API. Wave 2 candidates are Workable, Bullhorn, and Comeet after public token/config handling is reviewed. Remote/job-board aggregators such as Remotive, Himalayas, and Arbeitnow must stay separate from direct ATS adapters.

Detailed certification requirements live in [ATS Source Certification](./ats-source-certification.md). Data freshness and pruning rules live in [Data Retention](./data-retention.md).

## ATS Certification Workbench (v1.6.2)

The canonical evidence scoreboard lives in [ats-workbench/scoreboard.md](./ats-workbench/scoreboard.md) and [ats-workbench/scoreboard.json](./ats-workbench/scoreboard.json). Regenerate it with:

```bash
npm run audit:ats-quality -- --json --output=docs/reference/ats-workbench/scoreboard.json --markdown-output=docs/reference/ats-workbench/scoreboard.md
```

The workbench is read-only. It merges configured adapter metadata with production/test field-quality stats and adds `wave_priority`, `certification_blockers`, `exact_next_parser_action`, and `should_be_public_enabled` for every configured ATS key.

Current generated status counts:

- Configured ATS keys: 60.
- Certified: 23.
- Partial: 0.
- Fallback/pending: 36.
- Unsupported/disabled: 1 (`dayforcehcm`).

ATS-specific fetch/parser work packets are generated with `npm run ats:workbench`; the canonical index is `docs/reference/ats-workbench/index.json` and per-source records live under `docs/reference/ats-workbench/sources/`. For the direct JSON/API repair wave, source module paths are also recorded under each source's `source_module` and `runner_interface` fields.

Top quality-risk sources from the latest read-only production snapshot:

| Rank | ats_key | status | rows | missing any geo % | weak remote % | public-enabled recommendation | wave priority | exact next action |
| ---: | --- | --- | ---: | ---: | ---: | --- | --- | --- |
| 1 | `brassring` | fallback | 1531 | 100 | 98.04 | no | wave-1-live-gap | Add or run bounded detail-refetch certification for missing geo/remote evidence. |
| 2 | `teamtailor` | fallback | 2839 | 99.33 | 66.11 | no | wave-1-live-gap | Add saved raw response fixture, expected normalized fixture, invalid-shape rejection test. |
| 3 | `applitrack` | certified | 70598 | 100 | 99.98 | no | wave-1-live-gap | Add or run bounded detail-refetch certification for missing geo/remote evidence. |
| 4 | `hirebridge` | fallback | 3284 | 95.43 | 77.5 | no | wave-1-live-gap | Add or run bounded detail-refetch certification for missing geo/remote evidence. |
| 5 | `peopleforce` | fallback | 461 | 92.19 | 65.94 | no | wave-1-live-gap | Add saved raw response fixture, expected normalized fixture, invalid-shape rejection test. |
| 6 | `icims` | certified | 183222 | 95.7 | 78.24 | yes, quality debt | wave-1-live-gap | Add or run bounded detail-refetch certification for missing geo/remote evidence. |
| 7 | `pageup` | fallback | 462 | 93.94 | 71.43 | no | wave-1-live-gap | Add saved raw response fixture, expected normalized fixture, invalid-shape rejection test. |
| 8 | `careerspage` | fallback | 1875 | 94.03 | 32.37 | yes, quality debt | wave-1-live-gap | Add saved raw response fixture, expected normalized fixture, invalid-shape rejection test. |
| 9 | `applicantai` | fallback | 146 | 98.63 | 4.79 | yes, quality debt | wave-2-live-gap | Add saved raw response fixture, expected normalized fixture, invalid-shape rejection test. |
| 10 | `talentlyft` | fallback | 2041 | 94.12 | 3.04 | yes, quality debt | wave-2-live-gap | Add saved raw response fixture, expected normalized fixture, invalid-shape rejection test. |
| 11 | `gem` | fallback | 3493 | 96.54 | 19.24 | yes, quality debt | wave-2-live-gap | Add saved raw response fixture, expected normalized fixture, invalid-shape rejection test. |
| 12 | `jobvite` | fallback | 10090 | 95.64 | 12.71 | yes, quality debt | wave-2-live-gap | Add saved raw response fixture, expected normalized fixture, invalid-shape rejection test. |
| 13 | `jobaps` | fallback | 1256 | 67.12 | 60.83 | yes, quality debt | wave-2-live-gap | Add or run bounded detail-refetch certification for missing geo/remote evidence. |
| 14 | `talexio` | fallback | 262 | 85.88 | 27.86 | yes, quality debt | wave-2-live-gap | Add saved raw response fixture, expected normalized fixture, invalid-shape rejection test. |
| 15 | `recruitcrm` | certified | 2795 | 93.49 | 23.94 | yes, quality debt | wave-2-live-gap | Audit raw payloads and add field-specific parser/backfill fixture. |

Quarantine/disable recommendation until source-backed parser evidence improves: `brassring`, `teamtailor`, `applitrack`, `hirebridge`, `peopleforce`, `pageup`, plus no-row unproven sources that are currently enabled by configuration but lack strict raw parser fixtures (`adp_myjobs`, `calcareers`, `calopps`, `hibob`, `isolvisolvedhire`, `policeapp`, `sagehr`, `saphrcloud`, `statejobsny`, `theapplicantmanager`, `usajobs`). `dayforcehcm` remains unsupported/disabled.

Work-packet review notes from this pass:

- Direct JSON/API wave: `greenhouse`, `lever`, `ashby`, `smartrecruiters`, `recruitee`, `bamboohr`, `pinpointhq`, `recruitcrm`, and `fountain` are parser-fixture-backed and now have dedicated source-module folders. `manatal` and `zoho` also use dedicated source modules even though they are classified as vendor/embedded source families. `teamtailor`, `freshteam`, and `getro` still need raw fixture certification. `fountain` now preserves `id`/`opening_id`/`to_param` as `source_job_id`; add more path/pagination/sparse-field variants before broad source expansion.
- Enterprise/brittle wave: `workday`, `icims`, `taleo`, `oracle`, `paylocity`, `adp_workforcenow`, `adp_myjobs`, `ultipro`, `pageup`, `saphrcloud`, and `brassring` now have source-module folders, raw fixtures, expected normalized fixtures, and invalid-shape rejection/quarantine tests. `oracle`, `paylocity`, `adp_workforcenow`, `adp_myjobs`, `ultipro`, `saphrcloud`, and `brassring` now emit stable source ids from source payloads instead of relying only on URL extraction. `brassring` and `taleo` remain low-confidence; `pageup`, `saphrcloud`, and `ultipro` remain partial until canaries prove tenant variants. `dayforcehcm` stays disabled.
- Embedded/HTML wave: `icims` and `applitrack` are certified but remain large live field-gap sources that require bounded detail-refetch certification. `careerplug`, `applicantpro`, `applytojob`, `breezy`, and `zoho` have raw parser fixtures. `jobvite`, `theapplicantmanager`, `talentreef`, `hirebridge`, and `isolvisolvedhire` need raw fixture certification.
- Vendor/public-sector wave: `manatal` is parser-fixture-backed. Most vendor-specific and public-sector sources remain fallback/pending until raw fixtures prove source id, canonical URL, geo, date, and remote behavior.
