# Embedded Board ATS Certification Lane

This lane covers the embedded or semi-structured ATS tier from `server/ingestion/adapter-metadata.js`: `jobvite`, `icims`, `zoho`, `breezy`, `applicantpro`, `applytojob`, `theapplicantmanager`, `careerplug`, `talentreef`, `hirebridge`, `hrmdirect`, and `isolvisolvedhire`.

Certification standard: each ATS needs a source endpoint record, saved raw source fixture, parser regression, normalized fixture, and proof for `location_text`, `country`, `region`, `remote_type`, `posting_date`, `source_job_id`, and `last_seen_epoch`. Inline parser tests are useful evidence, but they do not replace saved raw source fixtures.

As of May 24, 2026, pure parsers belong under `server/ingestion/sources/<ats>/parse.js`. Rows that still mention `server/index.js` are pointing at legacy collector/discovery/fetch orchestration and should be updated to the source module path when that ATS is next touched.

## Current Records

| ATS | Source endpoint | Parser path | Raw fixture status | Field decisions | Tests needed |
| --- | --- | --- | --- | --- | --- |
| `jobvite` | `parseJobviteCompany` derives `https://jobs.jobvite.com/{company}/jobs` or `https://careers.jobvite.com/{company}/jobs`; `fetchJobviteJobsPage` reads HTML tables. | `server/ingestion/sources/jobvite/parse.js` `parseJobvitePostingsFromHtml`; legacy adapter wrapper in `server/ingestion/adapters.js`. | No saved `jobvite-direct.json` or normalized `jobvite-postings.json`. Inline direct parser coverage exists for grouped table rows and source-id extraction. | Geo comes from `td.jv-job-list-location`; date is always `null` unless future detail evidence is added; remote is inferred only by common normalizer from title/location text; source id is URL-derived from `/job/{id}`. | Add raw HTML fixture with multiple department groups, empty departments, remote and non-US locations, and detail-page date evidence if available. Add normalized fixture asserting country/region/remote/source id/date-null behavior. |
| `icims` | `parseIcimsCompany` normalizes to `/jobs/search?ss=1`; collector fetches wrapper HTML, iframe URL, paged iframe cards, and budgeted detail pages. | `server/index.js` `extractIcimsIframeUrlFromHtml`, `parseIcimsPostingsFromHtml`, `extractIcimsNextPageUrlFromHtml`, detail helpers; legacy adapter wrapper in `server/ingestion/adapters.js`. | Saved raw detail certification fixture exists: `server/ingestion/fixtures/icims-detail-certification.json`; normalized fixture exists: `server/ingestion/fixtures/icims-postings.json`. Wrapper/next-page fixture still pending. | Geo comes from card/detail labels first, including iCIMS `CC-state-city` and `CC-Remote` codes; date is stored only when source exposes it and many public detail pages omit it; remote uses explicit `Remote: Yes/No` first, then location/title text; source id is `/jobs/{number}`. Detail fetches fill missing geo/date/remote within `OPENJOBSLOTS_ICIMS_DETAIL_FETCH_LIMIT_PER_COMPANY`. | Add saved wrapper, iframe page, and next-page fixtures. Continue blank-location detail probes and date-present tenant fixtures if found. Cover duplicate page prevention and malformed/intro job links. |
| `zoho` | `parseZohoCompany` normalizes to `https://{tenant}.zohorecruit.com/jobs/Careers`; page embeds hidden `input#jobs` JSON plus optional `input#meta` list URL. | `server/ingestion/sources/zoho/parse.js` `parseZohoPostingsFromHtml`, `extractZohoHiddenInputValue`, `buildZohoJobUrl`; legacy adapter wrapper in `server/ingestion/adapters.js`. | Normalized fixture exists: `server/ingestion/fixtures/zoho-postings.json`. Inline direct parser coverage exists. No saved raw hidden-input fixture. | Geo is `City, State, Country`; date is `Date_Opened`; remote is inferred by normalizer only; source id is the Zoho `id`, also used in URL construction. Unpublished jobs are skipped. | Add saved raw hidden-input fixture with valid `meta`, no `meta`, malformed jobs JSON, unpublished jobs, country variants, blank city/state, and date variants. Assert normalized source id, country/region, date, and canonical URL. |
| `breezy` | `parseBreezyCompany` uses `https://{tenant}.breezy.hr/`; collector follows final portal URL and parses `/p/{id}` cards. | `server/ingestion/sources/breezy/parse.js` `parseBreezyPostingsFromHtml`, `extractBreezySourceId`; legacy adapter wrapper in `server/ingestion/adapters.js`. | Normalized fixture exists: `server/ingestion/fixtures/breezy-postings.json`. Inline direct parser coverage exists. No saved raw portal/detail fixture. | Geo comes from `li.location` or labeled card text; date comes from `li.posted` or labeled text and may be relative/null; remote is inferred from location/title text; source id is `/p/{id}`. Department is nearest preceding group header. | Add saved raw portal fixture for grouped cards, absolute and relative `/p/` links, relative date, missing date, hybrid/remote labels, and detail-page fallback evidence for fields omitted from list cards. |
| `applicantpro` | `parseApplicantProCompany` uses `https://{tenant}.applicantpro.com/jobs/`, extracts `domain_id`, then fetches `/core/jobs/{domain_id}?getParams={}` JSON. | `server/index.js` `parseApplicantProPostingsFromApi`, `extractApplicantProDomainId`, and `extractApplicantProLocationLabel`; legacy adapter wrapper in `server/ingestion/adapters.js`. | Raw API fixture exists: `server/ingestion/fixtures/applicantpro-direct.json`. Board HTML `domain_id` and `success=false` failure fixtures are still pending. | Geo comes from `jobLocation`, else `city, abbreviation/stateName, iso3`; date is `startDateRef`; remote is inferred by normalizer from title/location; source id is JSON `id` or `/jobs/{id}` fallback. | Add board HTML fixture for `domain_id`, relative/absolute URL variants, missing `jobUrl` fallback, `success=false` handling, and more city/state/country live variants. |
| `applytojob` | `parseApplyToJobCompany` uses `https://{tenant}.applytojob.com/apply`; collector parses modern list items and legacy Resumator links. | `server/ingestion/sources/applytojob/parse.js` `parseApplyToJobPostingsFromHtml`, `extractApplyToJobSourceId`; legacy adapter wrapper in `server/ingestion/adapters.js`. | Normalized fixture exists: `server/ingestion/fixtures/applytojob-postings.json`. Inline direct parser coverage exists. No saved raw HTML fixture. | Geo comes from map-marker icon, legacy location spans, or labeled text; date comes from calendar/clock icon, legacy posted span, or labeled text; remote is inferred by normalizer; source id is `/apply/{id}`. Department comes from labeled text when present. | Add saved raw fixtures for modern and legacy pages, detail-page field fallback, icon and label variants, missing date/department evidence, URL fragment canonicalization, and invalid/duplicate links. |
| `theapplicantmanager` | `parseTheApplicantManagerCompany` uses `https://theapplicantmanager.com/careers?co={companyCode}`. | `server/index.js` `parseTheApplicantManagerPostingsFromHtml`; not exported to direct parser tests yet. | No saved raw fixture and no normalized fixture. | Geo is currently `null`; date is `null`; remote cannot be inferred unless title/department text contains hints after normalization; source id is not explicitly set, so normalizer can only fall back to URL-derived behavior if available. Department comes from preceding bold `pos_title_list` paragraph. | Export parser or add direct fixture harness coverage. Add raw careers-page fixture with department headers, fallback link parsing, resume-link rejection, stable source-id extraction from URL, and a certification decision on whether detail fetch is required for date/location. |
| `careerplug` | `parseCareerplugCompany` uses `https://{tenant}.careerplug.com/jobs`; collector parses `aria-label` job links. | `server/ingestion/sources/careerplug/parse.js` `parseCareerplugPostingsFromHtml`; legacy adapter wrapper in `server/ingestion/adapters.js`. | No saved raw fixture or normalized fixture. Inline direct parser coverage exists for aria cards and source-id extraction. | Geo comes from `.job-location`; date is always `null`; remote is inferred by normalizer from title/location text; source id is `/jobs/{id}`. Employment type is captured from `.job-type`. | Add saved raw HTML fixture with aria/no-aria card variants, location formats like `OK-Sand Springs-74063`, remote labels, blank job type, and date-null proof. Add normalized fixture for source id, country/region, remote unknown/remote, and employment type. |
| `talentreef` | `parseTalentreefCompany` uses public board `https://apply.jobappnetwork.com/{company}`, alias API `/apply/careerPages/alias/{company}`, then search API `/apply/proxy-es/search-en-us/posting/_search`. | `server/index.js` `extractTalentreefAliasData`, `buildTalentreefSearchPayload`, `parseTalentreefPostingsFromSearchResponse`; not exported to direct parser tests yet. | No saved raw fixture and no normalized fixture. | Geo currently uses address city plus `stateOrProvinceFull/stateOrProvince`; country is not explicitly carried, so state-only rows can be country-sparse; date uses `createdDate`, `startDate`, or `updatedDate`; remote is inferred by normalizer; source id is not set even though `jobId` is available. | Export parser helpers. Add alias and search JSON fixtures with pagination, `hits.total`, `jobId` source id, brand filter, city/state/country variants, remote-only rows, and missing URL rejection. |
| `hirebridge` | `parseHirebridgeCompany` uses `https://recruit.hirebridge.com/v3/jobs/list.aspx?cid={cid}` and detail URL `https://recruit.hirebridge.com/v3/CareerCenter/v2/details.aspx?cid={cid}&jid={jid}`. | `server/ingestion/sources/hirebridge/parse.js` `parseHirebridgePostingsFromHtml`, `buildHirebridgeDetailsUrl`, `extractHirebridgeDatePostedFromDetailHtml`; not exported to direct parser tests yet. | No saved raw fixture and no normalized fixture. | Geo currently maps list `department` into both `location` and `department`, so department/location split is not certified; date requires detail page `datePosted`; remote is inferred by normalizer; source id is not explicitly set and should use `jid`. | Fix or document department/location split before certification. Add list and detail fixtures, `jid` source-id assertion, date detail extraction variants, no-detail failure behavior, and non-location department proof. |
| `hrmdirect` | `parseHrmDirectCompany` normalizes to `/employment/job-openings.php?search=true`; collector parses `reqitem` table rows. | `server/ingestion/sources/hrmdirect/parse.js` `parseHrmDirectPostingsFromHtml`; legacy adapter wrapper in `server/ingestion/adapters.js`. | Normalized fixture exists: `server/ingestion/fixtures/hrmdirect-postings.json`. Inline direct parser coverage exists. No saved raw table fixture. | Geo is `cities, state`; date is `td.date` or `td.dates`; remote is inferred by normalizer; source id is `req` or `reqid` query param. Department is `td.departments`. | Add saved raw table fixtures for `reqitem` and `reqitem1`, US city/state, non-US province/country, malformed href cleanup, missing date, and department. Assert source id, country/region, and date normalization. |
| `isolvisolvedhire` | Source-local module accepts `*.isolvedhire.com`, fetches board HTML, extracts `domain_id`, then calls `/core/jobs/{domain_id}?getParams=%7B%7D` JSON. | `server/ingestion/sources/isolvisolvedhire/parse.js` `extractIsolvisolvedhireDomainId`, `parseIsolvisolvedhirePostingsFromApi`; fetch/discovery live under `server/ingestion/sources/isolvisolvedhire/`. | Source-local raw fixture and expected-normalized fixture exist under `server/ingestion/sources/isolvisolvedhire/fixtures/`, including invalid-shape coverage. | Geo comes from `jobLocation`, `city`, `stateName`/`abbreviation`, and `iso3`; date is `startDateRef`; remote uses source `workplaceType`; source id is JSON `id` or URL fallback. Placeholder `00000, US` locations are kept country/remote-backed without fake city values. | Add more sparse `workplaceType` and country/territory variants before broader promotion; monitor live canary quality after deployment/source refresh. |

## Shared Test Requirements

- Add one `server/ingestion/fixtures/{ats}-direct.json` per ATS with the raw HTML or JSON response, parser config, and expected normalized fields.
- Add or extend direct parser coverage so each parser can run without live network calls. Extract pure parser helpers where the collector currently combines fetch and parse.
- Keep normalized fixtures for certification output: `{ats}-postings.json` should assert canonical URL, `country`, `region`, `remote_type`, `posting_date` or intentional null, `source_job_id`, and validation success.
- Add negative parser tests for missing title, company, or canonical URL, plus malformed raw payloads that should return no postings rather than invent fields.
- Run search parity after parser behavior changes: Postgres persistence, Meilisearch indexing, hydration, title search, country/region filters, remote filter, and canonical URL lookup.

