# Enterprise Direct ATS Certification Lane

Scope: enterprise/direct ATS keys from `server/ingestion/adapter-metadata.js`. This lane records the current source endpoint, parser path, raw fixture status, field decisions for geo/date/remote/source-id, and tests still needed before certification.

Certification remains blocked unless the source response is saved as a raw fixture and the normalized output proves `location_text`, `country`, `region`, `remote_type`, `posting_date`/`posted_at`, `source_job_id`, and `last_seen_epoch` behavior. Nullable geo/date/remote fields need raw evidence that the source omitted them.

## Coverage Snapshot

| ATS | Source endpoint | Parser path | Raw fixture status | Field decisions | Tests needed |
| --- | --- | --- | --- | --- | --- |
| `workday` | `POST https://{tenant}.myworkdayjobs.com/wday/cxs/{subdomain}/{companyId}/jobs` with `limit`, `offset`, empty `searchText`, and `appliedFacets`. Job URLs are rebuilt from `externalPath` under `https://{tenant}.myworkdayjobs.com/{companyId}`. | `server/index.js`: `parseWorkdayCompany`, `fetchWorkdayPage`, `collectTodayPostingsForWorkdayCompany`, `extractWorkdaySourceJobId`, `extractWorkdayLocationLabel`, `extractWorkdayRemoteSignal`. No exported pure CXS parser yet. | Pending saved raw CXS fixture. `server/ingestion/direct-parser-fixtures.test.js` has inline helper regression only. | Geo: prefer CXS location fields, then URL `/job/{location}/...` segment; normalize URL text such as `Work-From-Home`. Date: preserve Workday relative or absolute posting fields and let normalization parse them; do not filter to same-day only. Remote: derive from Workday remote/workplace/location fields and URL/title evidence. Source-id: prefer CXS id/requisition fields, then trailing `_JR...` from `externalPath` or URL. | Extract/export a pure `parseWorkdayPostingsFromCxs` parser; add saved `workday-direct.json` with active older postings, multiple pages, remote/work-from-home URL, onsite city/state/country, no-date row, and source-id variants; assert normalized geo/date/remote/source-id plus validation rejects missing title/company/URL; add pagination stop tests. |
| `oracle` | `GET {siteBaseUrl}/hcmRestApi/resources/latest/recruitingCEJobRequisitions?onlyData=true&expand=...&finder=findReqs;siteNumber={site},facetsList=...,limit={n},sortBy=POSTING_DATES_DESC&offset={offset}&limit={n}`. Board URLs are `.../hcmUI/CandidateExperience/{language}/sites/{site}/jobs`. | `server/index.js`: `parseOracleCompany`, `fetchOracleJobRequisitionsPage`, `parseOraclePostingsFromApi`, `buildOraclePostingUrl`, `extractOracleLocationFromRequisition`. | Strict saved raw fixture exists: `server/ingestion/fixtures/oracle-direct.json`, exercised by `server/ingestion/direct-parser-fixtures.test.js`. | Geo: prefer `PrimaryLocation`; otherwise combine `TownOrCity`, `Region2`, `Country`, or `LocationName` from `workLocation`. Date: currently requires `PostedDate`/`postDate`; no-date rows are skipped. Remote: use `WorkplaceType`/worker type/job type/contract type text for normalization. Source-id: requisition `Id` is embedded in the canonical URL; certification should also assert `source_job_id` explicitly. | Extend fixture set for alternate language/site, `hasMore` pagination, no-primary-location with nested `workLocation`, no-date behavior, duplicate `Id`, and remote/hybrid/onsite variants; add explicit `source_job_id`, `posting_date`, `country`, `region`, and `remote_type` assertions. |
| `adp_myjobs` | First `GET https://myjobs.adp.com/public/staffing/v1/career-site/{company}` for `myJobsToken` and `properties.myadpUrl`; then `GET {myadpUrl}/myadp_prefix/mycareer/public/staffing/v1/job-requisitions/apply-custom-filters?...` with `myjobstoken`, `$top`, and `$skip`. | `server/index.js`: `parseAdpMyjobsCompany`, `fetchAdpMyjobsCareerSite`, `fetchAdpMyjobsJobsPage`, `parseAdpMyjobsPostingsFromApi`, `formatAdpMyjobsLocation`. Parser is not exported for direct fixture tests. | Pending raw fixture; no saved direct fixture. | Geo: combine `requisitionLocations`, `workLocations`, and `postingLocations` name/address fields. Date: preserve `postingDate`, nullable when absent. Remote: currently relies on job `type`/employment text and location text; needs explicit remote/hybrid cases. Source-id: `reqId` should be the source id and is currently only guaranteed in fallback URL. | Export parser; add `adp_myjobs-direct.json` with career-site token response and jobs API response; test token-missing returns empty with parser attention called out separately; add pagination by `count`/`$skip`, location variants across all three location arrays, remote/hybrid text, explicit `reqId` source-id assertions, and validation rejection tests. |
| `adp_workforcenow` | `GET https://workforcenow.adp.com/mascsr/default/careercenter/public/events/staffing/v1/content-links/career-center?cid={cid}&ccId={ccId}&locale=en_US&lang=en_US`, then `GET .../job-requisitions?cid={cid}&ccId={ccId}`. Board URL is `.../recruitment.html?cid={cid}&ccId={ccId}`. | `server/index.js`: `parseAdpWorkforcenowCompany`, `fetchAdpWorkforcenowContentLinks`, `fetchAdpWorkforcenowJobsPage`, `resolveAdpWorkforcenowCompanyName`, `parseAdpWorkforcenowPostingsFromApi`, `extractAdpWorkforcenowLocation`. | Strict saved raw fixture exists: `server/ingestion/fixtures/adp_workforcenow-direct.json`, exercised by `server/ingestion/direct-parser-fixtures.test.js`. | Geo: combine requisition location label with address city/state/country. Date: preserve `postDate`, nullable when absent. Remote: use `workLevelCode.shortName` and location text for remote/hybrid normalization. Source-id: `itemID` should be preserved; current canonical URL usually carries `jobId`. | Extend fixtures for multiple requisition locations, no `contentLinks`, relative/alternate links, missing `postDate`, duplicate `itemID`, remote/hybrid/onsite variants, and explicit normalized `source_job_id`, `region`, `posting_date`, and `last_seen_epoch` assertions. |
| `adpworkforcenow` | Alias only. Canonical source is `adp_workforcenow`; URL and normalized ATS handling map `adpworkforcenow` to `adp_workforcenow`. | `server/index.js`: `normalizeAtsFilterValue`, `inferAtsFromJobPostingUrl`, and `extractSourceIdFromPostingUrl` alias branches; parser path is canonical `parseAdpWorkforcenowPostingsFromApi`. | Covered only through canonical `adp_workforcenow` fixture; no separate raw source should be created for the alias. | Geo/date/remote/source-id decisions are identical to `adp_workforcenow`. Store and certify the canonical key to avoid duplicate source identity. | Add alias tests that `adpworkforcenow` normalizes to `adp_workforcenow`, is not treated as a separate adapter, and still extracts source ids from ADP Workforce Now URLs. |
| `paylocity` | `GET https://recruiting.paylocity.com/recruiting/jobs/{listing}/{companyId}/{companySlug}` and parse embedded `window.pageData.Jobs`; canonical job URLs are `/Recruiting/Jobs/Details/{JobId}`. | `server/index.js`: `parsePaylocityCompany`, `fetchPaylocityBoardPage`, `extractPaylocityPageDataJson`, `parsePaylocityPostingsFromPageData`. | Strict saved raw fixture exists: `server/ingestion/fixtures/paylocity-direct.json`, exercised by `server/ingestion/direct-parser-fixtures.test.js`. | Geo: use `JobLocation.City`, `State`, `Country`, then `LocationName`, then remote fallback. Date: collector currently skips rows without `PublishedDate`; certification should document skip/null choice. Remote: use `IsRemote`, `LocationName`, and title/location text for remote/hybrid normalization. Source-id: `JobId` is embedded in details URL; should be asserted as `source_job_id`. | Extend fixtures for `IsRemote: true`, country-only rows, missing/malformed `Jobs`, missing `PublishedDate`, duplicate `JobId`, and hybrid text; assert explicit source id, region, posting date, and nullable-field evidence. |
| `dayforcehcm` | Configured URL patterns include `*.dayforcehcm.com/candidateportal/` and `careers.dayforcehcm.com/`, but no direct collector endpoint is implemented. | No parser implementation. Metadata marks it unsupported in `server/ingestion/adapter-metadata.js`; adapter tests confirm disabled/default exclusion. | Unsupported. No raw fixture. | Geo/date/remote/source-id decisions are intentionally unset until a source endpoint and parser are selected. Do not enable sync or certify normalized blanks. | Keep disabled; add implementation only after documenting a stable public endpoint, parser, raw fixture, expected normalized fixture, parser-adapter-not-implemented behavior, validation rejections, and production parity tests. |
| `eightfold` | `GET https://{host}.eightfold.ai/careers` to extract `window._EF_GROUP_ID`, then `GET https://{host}.eightfold.ai/api/pcsx/search?domain={groupId}&query=&location=&start=0&`. | `server/index.js`: `parseEightfoldCompany`, `fetchEightfoldCareersPage`, `extractEightfoldDomainFromHtml`, `buildEightfoldApiUrl`, `fetchEightfoldJobsApi`, `parseEightfoldPostingsFromApi`. Parser is not exported for direct fixture tests. | Pending raw fixture; no saved direct fixture. | Geo: use `positions[].locations` or string fallback; if absent and `workLocationOption` says remote, set location to `Remote`. Date: preserve `postedTs` as epoch/string. Remote: use `workLocationOption` and location text. Source-id: prefer `atsJobId` when present; otherwise `id`/URL id needs a documented fallback. | Export parser; add paired careers HTML plus API JSON fixture for group-id extraction; cover missing group-id error, pagination/start beyond zero, `atsJobId` vs `id`, remote-only roles, multiple locations, `postedTs` epoch parsing, and validation rejections. |
| `saphrcloud` | Preferred direct API is `POST https://{company}.jobs.hr.cloud.sap/services/recruiting/v1/jobs` with locale/page payload; current collector fetches board/search HTML and uses HTML parser instead of the API parser. | `server/index.js`: `parseSapHrCloudCompany`, `buildSapHrCloudSearchPayload`, `fetchSapHrCloudJobsPage`, `parseSapHrCloudPostingsFromApi`, `fetchSapHrCloudBoardPage`, `parseSapHrCloudPostingsFromHtml`, `buildSapHrCloudJobUrl`. API parser is present but unused by collector. | Pending raw fixture; no saved direct fixture. | Geo: API parser prefers `jobLocationShort`, coordinate labels, state, then country; HTML parser reads `jobLocation`/city section. Date: API uses `unifiedStandardStart`, `postedDate`, `publishDate`, or `startDate`; HTML uses `jobDate`. Remote: not explicit; needs fixture-backed remote/hybrid decision from source fields/text. Source-id: API `id` is encoded in job URL; should also be asserted as `source_job_id`. | Decide API-first vs HTML fallback; add raw API fixture for `/services/recruiting/v1/jobs` and fallback HTML fixture; cover locale/date formats, no-location rows, remote/hybrid text, source-id from `id`, pagination by `pageNumber`, and validation rejections. |
| `ultipro` | `POST https://recruiting.ultipro.com/{tenant}/JobBoard/{boardId}/JobBoardView/LoadSearchResults` with `opportunitySearch.Top`, `Skip`, posted-date sort, and empty filters. | `server/index.js`: `parseUltiProCompany`, `buildUltiProSearchPayload`, `fetchUltiProSearchResults`, `collectPostingsForUltiProCompany`, `extractUltiProLocationName`. Parser is inline in collector, not exported. | Pending raw fixture; no saved direct fixture. | Geo: combine `Locations[].Address.City`, `State.Code`, `Country.Name`, or localized fallback. Date: preserve `PostedDate`. Remote: currently weak; depends on location/title text unless source exposes a workplace flag in fixture. Source-id: `Id` becomes `opportunityId` in URL but should be preserved explicitly. | Extract/export `parseUltiProPostingsFromApi`; add `ultipro-direct.json` with `opportunities`, `totalCount`, pagination, multi-location, country-only, no-date, remote/hybrid cases, explicit `Id` source id, and validation rejections. |
| `pageup` | `GET https://careers.pageuppeople.com/{boardId}`; then `POST https://careers.pageuppeople.com/{boardId}/{cw|ci}/{locale}/search/` for JSON containing `results` HTML; then each job detail page is fetched for posting date evidence. | `server/index.js`: `parsePageupCompany`, `fetchPageupBoardPage`, `extractPageupRouteConfigFromUrl`, `fetchPageupSearchResults`, `parsePageupPostingsFromResults`, `fetchPageupDetailsPage`, `extractPageupPostingDateFromDetailHtml`. Parser is not exported for direct fixture tests. | Pending raw fixture; no saved direct fixture. | Geo: parse listing row `.location` text. Date: prefer detail-page date; listing date fallback exists but collector currently requires detail date and skips failures. Remote: infer from location/title text unless PageUp source fields expose a direct remote flag. Source-id: extract `/job/{id}` from URL as `external_id`; should become `source_job_id`. | Export parser helpers; add two-part fixture set for board HTML, search JSON/results HTML, and detail HTML; cover listing-date fallback, detail fetch failure policy, route/locale changes, source id from `/job/{id}`, remote/hybrid locations, pagination if source exposes it, and validation rejections. |

## Cross-Cutting Tests

- Run `node --test server/ingestion/direct-parser-fixtures.test.js` after adding each saved raw fixture.
- Add adapter metadata tests for fixture status transitions only when a raw parser fixture is actually added.
- Add production parity coverage for enterprise changes that alter normalized title, location, country, region, remote type, posting date, source id, or canonical URL: Postgres persistence, Meilisearch indexing, hydration, public search, and filters.
- Add negative parser tests for missing title, company, canonical URL, malformed raw payloads, duplicate source ids, and nullable geo/date/remote evidence.
